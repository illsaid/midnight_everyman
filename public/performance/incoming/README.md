# Incoming performance clips

Place the selected, unkeyed chroma acquisition clip here as:

```text
observer-reach-stop-recoil-green-v1.mp4
```

The Sprint 1 composition accepts the relative public path:

```text
performance/incoming/observer-reach-stop-recoil-green-v1.mp4
```

This is an intake location, not the final reusable library. An approved clip is keyed and normalized once, then promoted with metadata after the comparison passes.
